return {
  "Grafcube/suedit.nvim",
  lazy = false,
  dependencies = "akinsho/toggleterm.nvim",
  opts = { cmd = "sudo" }, -- default
  -- eg: { cmd = "doas" }
  -- eg: { cmd = "run0" }
  config = function()
  end,
}
